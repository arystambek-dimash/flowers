# Сюда кладём фото

Положи один файл с именем `1.jpg` — он попадёт в пазл.
Квадратный снимок смотрится лучше всего.

Пока файла нет, на странице показывается нарисованная заглушка.
Путь и имя меняются в `CONFIG.photo` в `index.html`.
